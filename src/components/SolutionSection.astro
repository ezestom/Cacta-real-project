---
import { Image } from "astro:assets";
import tractor from "../img/tractor5.jpg";
import Gallery from "./Gallery.astro";
---

<section
	class="w-screen h-screen text-center bg-black/90 relative overflow-hidden text-white z-40 flex justify-end items-center"
	data-header-color="black"
	id="solution">
	<article
		class="z-20 top-0 bottom-0 w-full lg:w-1/2 h-full px-2 p-0 justify-evenly md:justify-center items-center
		flex flex-col">
		<aside class="text-start px-8">
			<h5>
				<span class="text-2xl font-bold text-white">Solución</span>
			</h5>
			<hr />
			<h6 class="text-xl font-semibold text-white">
				Gestionar la <span>sostenibilidad</span>
				nunca fue tan fácil.
			</h6>
			<p class="font-normal text-white text-pretty max-w-[55ch]">
				Alcanza tus objetivos sin complicaciones. Recopila y analiza tus
				datos de forma automática, y sencilla.
			</p>
		</aside>
		<Gallery />
	</article>
	<div class="absolute bottom-0 top-0">
		<Image class="bg-image1" src={tractor} alt="Image of tractor" />
	</div>
</section>

<style>
	aside {
		backdrop-filter: blur(5px);
		border-radius: 0.5rem;
		padding: 0.5rem 1rem;
		background-color: rgba(255, 255, 255, 0.1);
		box-shadow: 0 0 10px 0 rgba(0, 0, 0, 0.1);
		filter: brightness(1.1);
	}
	hr {
		/* margin-left: 2rem; */
		height: 1px;
		margin-bottom: 0.5rem;
		width: 8ch;
		border: none;
		background: linear-gradient(
			to right,
			transparent 0%,
			#4daa57 50%,
			transparent 100%
		);
	}
	span {
		color: #4daa57;
		font-weight: 800;
	}
	.bg-image1 {
		object-fit: cover;
		object-position: center;
		mask: radial-gradient(circle at right, white 30%, transparent 100%);

		width: 100vw;
		height: 100%;
		/*  invert image */
		transform: scaleX(-1);
	}
	@media (width < 768px) {
		.bg-image1 {
			mask: radial-gradient(circle at right, white 0%, transparent 100%);
		}
	}
</style>
