---
import { Form } from "./Form/Form";
import { Image } from "astro:assets";
import tractor from "../img/cow.jpg";
import LandingFooter from "./LandingFooter.astro";
import bg from "../icons/bg.svg";
---

<section
	class="w-screen h-screen text-center bg-black/90 overflow-hidden text-black flex justify-start items-start relative"
	data-header-color="black"
	id="contact">
	<article
		class="z-20 top-0 md:pl-4 bottom-0 w-full md:w-1/2 h-full justify-center items-start
		flex flex-col">
		<!-- svg -->
		<img
			class="absolute left-0 top-0 h-screen brightness-200"
			src={bg.src}
			alt="bg svg"
		/>
		<!-- svg -->
		<aside class="text-start px-8">
			<h5>
				<span class="text-2xl font-bold text-white">Contacto</span>
			</h5>
			<hr />
			<h6 class="text-xl font-semibold text-white">
				Gestionar la <span>sostenibilidad</span>
				nunca fue tan fácil.
			</h6>
			<p class="font-normal text-white text-pretty max-w-[55ch]">
				Descubre la sostenibilidad en la agricultura con Cacta Software.
				Simplificamos la medición de prácticas eco-amigables para que
				alcances tus objetivos sin complicaciones.
			</p>
			<p
				class="font-normal pt-1 text-white text-pretty pb-4 max-w-[55ch]">
				Contacta con nosotros y transforma tu enfoque hacia la
				sostenibilidad agrícola.
			</p>
			<Form client:visible />
		</aside>
	</article>

	<div class="absolute bottom-0 top-0 h-screen w-full">
		<Image class="bg-image1" src={tractor} alt="Image of cow" />
	</div>
	<LandingFooter />
</section>

<style>
	.bg-image1 {
		z-index: -10;
		width: 100%;
		height: 100%;
		object-fit: cover;
		object-position: center;

		mask: radial-gradient(circle at right, white 30%, transparent 100%);
	}
	hr {
		/* margin-left: 2rem; */
		height: 1px;
		margin-bottom: 0.5rem;
		width: 15ch;
		border: none;
		background: linear-gradient(
			to right,
			transparent 0%,
			#4daa57 50%,
			transparent 100%
		);
	}
	span {
		color: #4daa57;
		font-weight: 800;
	}
</style>
